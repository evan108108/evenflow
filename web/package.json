{
  "name": "evenflow-web",
  "version": "0.1.0",
  "private": true,
  "description": "Evenflow webapp — Solid + Effect SPA served by the evenflow Worker",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "NODE_OPTIONS=--no-experimental-webstorage vitest run"
  },
  "dependencies": {
    "@fontsource-variable/bodoni-moda": "^5.2.0",
    "@fontsource-variable/dm-sans": "^5.2.0",
    "@solidjs/router": "^0.15.0",
    "effect": "^3.11.10",
    "solid-js": "^1.9.0"
  },
  "devDependencies": {
    "jsdom": "^26.0.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.0",
    "vite-plugin-solid": "^2.11.0",
    "vitest": "^3.2.7"
  }
}
